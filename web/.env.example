# ----- Frontend (read by Vite dev and injected by Docker runtime) -----
# JSON array of per-chain deployment overrides. The web app merges these
# values with its built-in chain registry at startup.
CHAIN_CONFIGS=[{"chainId":42161,"vaultAddress":"0x949556cb8634F9a4a8504665C3d0D9d326c600b2","graphUrl":"","rpcUrl":"https://solitary-empty-shard.arbitrum-mainnet.quiknode.pro/c3231ec35435fecf285eaa7e4b5010dc75881ec0/"}]

# ----- Docker runtime (standalone web-only image) -----
RELAYER_URL=http://relayer:3000
VALIDATOR_SERVICE_URL=http://validator-service:3001

# ----- Docker host port -----
HOST_PORT=8080
